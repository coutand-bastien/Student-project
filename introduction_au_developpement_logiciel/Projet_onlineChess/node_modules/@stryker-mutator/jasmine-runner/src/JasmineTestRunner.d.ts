import { TestRunner, RunResult } from '@stryker-mutator/api/test_runner';
import { StrykerOptions } from '@stryker-mutator/api/core';
export default class JasmineTestRunner implements TestRunner {
    private readonly fileNames;
    private readonly jasmineConfigFile;
    private readonly Date;
    static inject: ["sandboxFileNames", "options"];
    constructor(fileNames: ReadonlyArray<string>, options: StrykerOptions);
    run(options: {
        testHooks?: string;
    }): Promise<RunResult>;
    private createJasmineRunner;
    clearRequireCache(): void;
}
//# sourceMappingURL=JasmineTestRunner.d.ts.map