import { Transpiler } from '@stryker-mutator/api/transpile';
import { File, StrykerOptions } from '@stryker-mutator/api/core';
import { LoggerFactoryMethod } from '@stryker-mutator/api/logging';
export default class TypescriptTranspiler implements Transpiler {
    private readonly options;
    private readonly produceSourceMaps;
    private readonly getLogger;
    private languageService;
    private readonly filter;
    static inject: ["options", "produceSourceMaps", "getLogger"];
    constructor(options: StrykerOptions, produceSourceMaps: boolean, getLogger: LoggerFactoryMethod);
    transpile(files: ReadonlyArray<File>): Promise<ReadonlyArray<File>>;
    private filterIsIncluded;
    private createLanguageService;
    private transpileFiles;
}
//# sourceMappingURL=TypescriptTranspiler.d.ts.map